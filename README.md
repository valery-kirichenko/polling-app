Example `config.json`:
```JSON
{
    "production": {
        "url": "mongodb://user:password@server:port/db",
        "collection": "polls"
    },
    "development": {
        "url": "mongodb://user:password@server:port/db",
        "collection": "tests"
    }
}
```
